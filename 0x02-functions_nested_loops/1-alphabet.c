#include "main.h"
/**
 * mian: A Function that prints the alphabet in Lower case
 *
 * Return: Always to 0 (Success)
*/

void rx_nemeks(void)
{
 char n;
 for (n = 'a'; c <= 'z'; n++)
       _putchar(n);
 _putchar('\n');
}
